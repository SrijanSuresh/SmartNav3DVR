using UnityEngine;
public class CartDebugHotkeys : MonoBehaviour
{
    public EllipseMover cart;
    void Update()
    {
        // if (Input.GetKeyDown(KeyCode.T)) cart.moveEnabled = !cart.moveEnabled;
        // if (Input.GetKeyDown(KeyCode.Y)) cart.ResetCart();
    }
}
